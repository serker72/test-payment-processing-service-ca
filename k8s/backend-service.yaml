apiVersion: v1
kind: Service
metadata:
  name: payment-processing-backend
  namespace: payment-processing
  labels:
    app: payment-processing-backend
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: payment-processing-backend
